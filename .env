# .env.example

# Port the Express server will listen on. 
# PORT=3000

# API Key used by the 'authMiddleware' for authentication via the 
# 'X-API-KEY' (12345) header. This is compared against the hardcoded value 
# 'your-secret-api-key' in server.js.
# API_KEY=your-secret-api-key
# How to Use Environment Variables
# For my code to actually use these environment variables, you would typically:
# Install dotenv: Run npm install dotenv.
# Create .env: Create a file named .env (and add it to your .gitignore) where you put the actual secret values:

# updating the server.js file to match the .env.example file
# // At the very top of server.js
# require('dotenv').config();

# // ... later in the file ...
# const PORT = process.env.PORT || 3000;

# // ... update authMiddleware ...
# const authMiddleware = (req, res, next) => {
#   const apiKey = req.headers['x-api-key'];
#   // Use the environment variable here
#   if (!apiKey || apiKey !== process.env.API_KEY) { 
#     return next(new ValidationError('Invalid or missing API key'));
#   }
#   next();
# };
PORT=5000
MONGO_URI=mongodb://localhost:27017/week2